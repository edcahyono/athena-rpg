{
  "_readme": "Master RAG source. Each chunk = one approved Q&A pair (or tight fact cluster). `personas` lists which FOLDERS receive the chunk — the seven executives, the seven gk-* gatekeepers, or `general` (shared with every executive). Executive folders are retrieved from; gatekeeper folders are compiled whole into track.knowledge. Managed through the coach console at /coach; re-run `npm run ingest` after editing this file by hand.",
  "chunks": []
}